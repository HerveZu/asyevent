specification if only the matched signatures callbacks should be raised.
add et remove callbacks with EventWrapper methods.
kwargs for specification if automatic parameters are passed.
use "is not None" instead of "not"
personal error handler for every event and callback
reformatting using Black
import typing instead of ... from typing

parsing error raised even with handler